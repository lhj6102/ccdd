# Script conventions

Keep rendering and review output outside the input folders. Read artifacts through the supplied scope. Use fixed commands, validate input, and keep stdout limited to one ToolResult JSON object. Use stderr for diagnostics.
