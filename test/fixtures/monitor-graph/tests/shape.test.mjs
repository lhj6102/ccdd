import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';test('개발 검증: 테스트 파일과 계약 문서',()=>{assert.match(readFileSync(new URL('./focus.test.mjs',import.meta.url),'utf8'),/완료/);assert.match(readFileSync(new URL('../spec/spec.md',import.meta.url),'utf8'),/원본/);});
