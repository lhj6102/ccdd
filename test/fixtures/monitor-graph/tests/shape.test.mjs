import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';test('development validation: test files and contract document',()=>{assert.match(readFileSync(new URL('./focus.test.mjs',import.meta.url),'utf8'),/completed/);assert.match(readFileSync(new URL('../spec/spec.md',import.meta.url),'utf8'),/original/);});
