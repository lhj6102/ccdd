> Historical verification of v0.1/v0.2. This file describes the previous commit/server demo; use README.md and contracts.md for v0.3.

# v0.2.0 verification

Verified on 2026-09-05 with Node.js 24.15.0 and Codex CLI 0.153.4.

- Actual `doctor` execution received a response using `gpt-6-astra` / `medium`, verified a call that read a random diagnostic value through Artifact MCP, and returned READY. The two identical Agent Profiles were diagnosed with one call.
- An Agent diagnostic configured with an invalid Codex executable path returned `PROVIDER_NOT_EXECUTABLE`, NOT_READY, and exit code 1.
- With the same invalid Codex path, a diagnostic selecting only the Runtime Critic returned READY and exit code 0. It did not execute project tests.
- Running only the actual `tests-spec` Agent Critic returned GREEN and exit code 0. The Run recorded 1 request, `scope.kind=critic`, and `predecessorId=null`. Spec and Tests Viewer call evidence was checked.
- Actual browser checks covered GREEN and RED results from running only the Runtime Critic, excluded Critic indicators, manual readiness diagnostics, and scope restoration through history and refresh. Automatic polling did not repeat diagnostics, and there were no browser errors.
- All 51 automated checks passed. Coverage included authentication, model, network, and MCP errors; rejection of fabricated diagnostic responses; diagnostic cleanup; selected scope; repeated snapshots; completion waiting and exit codes; and the npm bin symlink entry point. Provider test doubles in automated checks were distinguished from the actual account verification above.

- The `ccdd` symlink in a separate npm installation displayed help, returned Runtime READY, and returned NOT_READY when Codex was missing. Diagnostic worktrees were cleaned up, and authentication and execution state were excluded from the package.

[Installation package and actual verification records](https://github.com/lhj6102/ccdd/releases/tag/v0.2.0) · [Builder workflow](builder-workflow.md)
