# Version 4 migration verification

Verified on 2026-09-24 with Node.js 22.23.2 on Linux x64. This record covers the folder-owned Artifact migration, not a published release or real Provider evaluation.

## Automated checks

- `npm test`: 245 tests passed, with no failures, skips, cancellations or TODOs. This includes the English-language check, TypeScript build, monitor type checking/build and Node's built-in test runner.
- `npm run test:packages`: passed using the local npm cache. All three 4.0.0 tarballs were packed, checked and installed into temporary production projects with install scripts disabled.
- `git diff --check`: passed.

Coverage includes nested discovery, unique names, excluded directories, JSON-only loading, instruction escapes/aliases, absent references, mount collisions and repeated/cyclic connections. Actual Node script processes exercise standard stdin/context, schema validation, text/JSON/image results, audit failures, timeouts, cancellation and descendant cleanup. Actual Runtime tests verify cyclic selection, partial INCOMPLETE results, recursive completion, reuse and semantic failures. Material, script, environment, mount and instruction changes affect their consuming identities while unrelated Artifacts retain applicable evidence.

Broker and CLI checks exercise separate workers, concurrent cyclic Critics, bounded execution, Human reservations/claims, input mutation and restoration, worker death, cancellation and authoritative completion. Readonly status and monitor GET tests confirm that neither scripts nor stored-state reconciliation run. Historical input versions are readable but cannot provide current evidence or resume execution.

Package checks exercise both default and custom script tools, actual Runtime verification followed by reuse, and the shipped custom-reader/computed-view examples. The default-tools installation also exercises the folder/mount example's real anonymous image-pair script and its installed `ccdd-view` reader. These are observations of controlled materials, not quality verdicts.

## Browser check

Opened the built monitor on a temporary project containing a child Artifact, a mutual reference/mount cycle, an explicit basis and an Artifact without Critics. Actual Node tests produced the saved Runtime results. Verified folder paths, typed relations, dashed cyclic edges, owned Critic icons, fitting the graph and current-input UNREVIEWED display. The temporary server, input, state and browser tab were removed afterward.

## Evidence limits

No external Provider was called, and no real Human verdict or desktop application launch was performed for this migration. Provider/Human lifecycle tests use explicitly controlled transports, callers and launcher fixtures. They do not establish live authentication, model access or reviewer availability. Runtime and script execution, package installation, HTTP behavior and the browser check were real executions against temporary fixtures.

The sandbox's subprocess stdin restriction caused script-test failures in a restricted initial run. The reported complete suite and package checks ran with ordinary local process access; all final results above are from that environment. No authentication data, SQLite files, raw Provider event logs or generated review state is included in the source changes.
