# v0.4.0 verification

Validated on macOS with Node 24.15.0. Filesystem watcher checks ran with native filesystem access, as in v0.3.

## Automated checks

**93 tests passed, 0 failed.** Coverage includes actual subprocesses, stdio MCP, and filesystem changes.

- Type-level operation descriptions validate before acceptance, preserve existing configurations, and replace every `{artifactName}` literally.
- Generated tools retain `read_spec`, `list_tests`, and `read_tests`; their descriptions exactly match the rendered templates.
- File and directory read schemas enforce distinct path requirements and reject old byte-read arguments.
- Line pagination covers defaults and bounds, LF/CRLF, Korean and emoji, BOM preservation, empty files, EOF, chunk boundaries, and the 64KiB response limit.
- Invalid UTF-8, oversized single lines, symlinks, and non-regular files fail explicitly.
- Successful MCP reads retain observation metadata. Listing alone and reading past EOF of a nonempty file cannot satisfy required inspection.
- Existing lock/copy, independent workers, Human waiting/completion, runtime execution, and readiness checks remain covered.

An independent reference comparison also passed 1,008 combinations of line ranges and file contents.

## Actual Provider and runtime

The existing Codex gpt-6-astra / medium profile performed real diagnostic and semantic calls. No credentials or raw Provider event streams are included in this verification record.

| Check | Result | Evidence |
| --- | --- | --- |
| Full-project doctor | READY | Random nonce returned through a successful `read_ccdd_probe_*` call with startLine 1 and lineCount 1. |
| Spec → Why | GREEN | Agent called `read_spec` and `read_why`; recorded observations contain 18 and 8 lines. |
| Tests → Spec | GREEN | Agent called `read_spec`, `list_tests`, and `read_tests`; the latter read all 10 test source lines through its internal path. |
| Implementation → Tests | GREEN | All six actual Node tests passed. |
| CLI partial read | PASS | Tests lines 3–5 matched the original text exactly and returned nextStartLine 6. |

Full-chain Review Handle: `f631c11b-1220-4dee-a202-55cdb90d5288`.
Snapshot hash: `f2fb5e2f9e017d26a703b0c4e48914dc43f464e71d3f5ff26bdbcfe6d53ba956`.

The generated `read_tests` description exactly matched the configured template. Its English translation is “Read source text from tests by line.” The demo config defines that template under `artifactTypes.code.tools.read.description`.

## Installed package

A candidate npm tarball was installed into a separate temporary directory. Its actual `.bin/ccdd` executable reported 0.4.0. In a plain non-Git workspace it returned runtime doctor READY, ran a runtime review GREEN, preserved Korean/emoji/CRLF in a one-line read, and read/listed a directory Artifact. Legacy byte-read arguments returned exit code 2.

The package includes the shared Artifact type validator and excludes state, authentication, generated output, server and UI files. The final release package may add this verification document; its executable sources must match the independently tested candidate.

## Compatibility

Tool names remain `{toolName}_{artifactName}`. Existing type definitions without custom descriptions continue to work. Read callers must replace `offset`/`limit` with `startLine`/`lineCount`; listing retains entry pagination. Fresh demo preparation uses demo-v4 and does not overwrite prior demo directories.
