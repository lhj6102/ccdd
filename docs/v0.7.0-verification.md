# v0.7.0 verification record

Verified on 2026-09-06 (Asia/Seoul). Server TypeScript and Vue component checks ran on Node.js 24.15.0.

## Automated checks

`npm test`: **164 passed, no failures or skips**. The server uses TypeScript 7.0.2; Vue SFCs use a separate TypeScript 6.0.3 supported by vue-tsc. The build uses explicit paths for each compiler to avoid npm executable alias conflicts.

- Explicit Agent/Human tool lists, rejection of requests with empty audience tool lists, individual Critic selection, and compatibility when browsing existing snapshots.
- Execution with registered fixed arguments, Artifact path restrictions, input changes during execution, cancellation, timeouts, descendant process cleanup, and prevention of output and Provider environment variable exposure.
- Tool preflight and actual execution, short tool names and exposed full names, and checks that create no review history.
- Browser claimant and CSRF checks, duplicate action and verdict prevention, matching snapshot and request scope, and handling of 129-character tool names.
- Claimant persistence across server restarts, copied reviews whose original source is missing, and completion of independent successor Runtime work after server shutdown.
- Regressions for workspace lock/copy, required Agent observations and structured verdicts, durable Human waiting, and actual Node test execution.

## Browser checks

Existing actual Astra review records and a separate UI validation project were connected together. UI validation verdicts were explicitly marked as test records and did not replace Human quality verdicts for an actual project.

- Project selection and all four Kanban columns were checked at 1280×850.
- After a Human claim, the request card moved to the running column.
- A single tool button click read a file; the next-lines action continued from line 81.
- The `open` tool actually opened `spec.md` from the review copy in CotEditor. The copy path differed from the original workspace path.
- The verdict draft persisted during periodic refreshes and program launches.
- After GREEN, a summary, and evidence were submitted through the UI, the actual successor Node tests also completed GREEN.
- Spec and Tests directories and files from existing Agent reviews remained readable.
- The detail view and board were checked at 390×844. Document width was 390px with no horizontal overflow. There were no console errors or warnings.

## Installation boundary

Installation checks use the generated npm archive in a separate path with production dependencies only and installation scripts disabled, covering the CLI, monitor assets, and tool checks. The Vue UI uses packaged JS/CSS without an external CDN; Vite, TypeScript, and vue-tsc are not required at runtime. Archive checksums and installation validation results are retained separately in the release verification materials.

## Scope

This change made no new live Provider calls. Checks covered Agent execution contract regressions and compatibility with existing actual Astra records. Human program preflight checks executable existence, execution permissions, and the Artifact path. Successful application launches are verified through `tools check --execute` or an explicit tool button action. A successful launch does not itself establish that a person read the content or submitted a verdict.

New Agent/Human requests require tool lists for each audience. New default demos are created in `demo-v7` to preserve existing demos.
