# v0.5.1 verification

Validated on macOS with Node 24.15.0 and Pi Agent Core / Pi AI 0.85.1.

## Update and regression checks

Both Pi packages are pinned to 0.85.1. The installed catalog resolves `gpt-6-astra` for `openai` and `openai-codex`; CCDD accepts exact low/medium/high/xhigh/max reasoning and rejects off/minimal/ultra. No model or reasoning fallback was added.

Strict TypeScript checking and all **114 tests** passed. The real Pi Agent loop with scripted transport now covers Astra, the existing GPT-5.6 Sol profile, and Anthropic. Artifact validation/auditing, final schema checks, cancellation, copy/lock integrity, and Human persistence regressions remain covered.

New demos use `openai-codex / gpt-6-astra / medium` in `demo-v5.1`. The manifest format remains version 5. Existing demo files and profiles are preserved; the installed CLI check confirmed that generating a fresh default demo leaves an existing sibling `demo-v5` untouched.

## Actual installed-package execution

A production-only candidate tarball was installed in a separate temporary prefix. All live checks below used its actual `ccdd` executable and explicitly selected an existing Codex access token through the read-only bridge.

| Check | Result | Evidence |
| --- | --- | --- |
| Full-project doctor | READY | Astra returned the random nonce after a successful Artifact tool read. Authentication, model access, and Artifact tools all passed. |
| Fixed, copy, full chain | GREEN | Both Agent Critics read all declared Artifacts; the runtime passed six actual Node tests. |
| Why change, lock, spec-why only | RED | Astra identified maximum 2 in Why versus maximum 3 in Spec. |

The exact live profile was `openai-codex / gpt-6-astra / medium`. Live account access for the separate `openai` API provider and other reasoning levels was not exercised.

- Fixed review: `2dc849d8-8802-46d5-bb73-a18a7b0b3f8a`
- Why-change review: `456c81e6-6389-4034-9605-f37607d13d1c`
- Fixed snapshot: `f86e25bd02474f3c32d897026ea6d69ee6506fcaaca5d4493e3a5bf18206e729`

## Installation and package integrity

The independent installation had both Pi packages at 0.85.1 and no Codex CLI or TypeScript compiler. Runtime diagnosis and review passed. Human copy mode reached ownerless WAITING_HUMAN, returned exactly line 2 of a CRLF Artifact, and resumed its runtime successor to GREEN from a separate CLI process. Worker settings retained credential paths without credential contents.

SHA-256 checksums were recorded for all 57 packaged compiled-code, declaration, and source-map files. The final release package is compared with these tested files; this verification document does not change executable code. The release includes a sanitized verification JSON with actual results and observation metadata.
