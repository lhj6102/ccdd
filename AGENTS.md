# CCDD

Write documentation, code comments, user-facing messages, review prompts, examples, and test descriptions in English. Preserve intentional Unicode test data with escapes and retain encoding coverage. Keep the README accessible to newcomers; place detailed contracts in linked docs. See CONTRIBUTING.md for contribution and verification guidance.

Read docs/contracts.md and CONTEXT-MAP.md before changing boundaries.
Definition-only @ccdd/core, project tool @ccdd/project and optional @ccdd/default-tools library; Project Validation, Broker and Executors are separate bounded contexts. Project queries recursively derive freshness from Artifact identities and actual review evidence, with per-query memoization and no persistent staleState or push invalidation. Critics reuse only matching actual PASS evidence for their effective definition, target and direct deps; unchanged intermediates stop downstream re-execution. TS config explicitly registers tool definitions; importing a library never registers or executes tools. Default Agent tools wrap a packaged CLI; default Human tools open desktop programs. Custom tools implement the same metadata/execute contract. Artifact Runner resolves request payload references into scoped reviewer-specific tools. Reviews use whole-workspace lock or immutable content-hash copies. Request-scoped workers own execution; the optional loopback monitor delegates explicit current-input inspection to Project Validation and Human claim, tool, and result actions to the Broker. GET requests never evaluate user config, execute user tools, reconcile, or mutate stored review state. Store serializable tool manifests and reconnect implementations from the same snapshot. Keep state and outputs outside reviewed input.
Critics declare one target Artifact and dependency Artifacts; the graph derives from these roles. Project verification gates on all required Critic results for each dependency and creates only ready tickets. Individual verification runs ready selected Critics and reports blocked scope INCOMPLETE; recursive verification includes required ancestors. Legacy ccdd graph runs preserve same-Run gates. The demo stays linear: Why → Spec → Tests → Implementation; the engine and monitor support general Artifact DAGs.
Keep actual provider evaluation and runtime execution; never fabricate recorded review results. Use Node's built-in test runner. Do not commit authentication data, SQLite state, worktrees, or raw provider event logs.
